# GENERATED by scripts/qa/compile-flow.mjs — DO NOT EDIT.
# Source of truth: qa/journey.json + qa/selectors.json.
# Re-generate:  node scripts/qa/compile-flow.mjs
# Validate:     node scripts/qa/lint-flows.mjs   (fails if this file is stale)
#
# STORE is supplied per-run via --env=STORE=<store>. There is deliberately NO
# flow-level env: block — a baked default out-ranks the CLI flag and writes
# captures to the wrong store dir (hard-won; see runbooks/qa-capture.md).

appId: com.joshapproved.tend
---
- launchApp:
    clearState: false
- extendedWaitUntil:
    visible: { text: "Today" }
    timeout: 30000
- tapOn:
    text: "People.*"
    retryTapIfNoChange: true
- extendedWaitUntil:
    visible: { text: "Mom.*" }
    timeout: 15000
- assertNotVisible: { text: "Marcus.*" }
- tapOn:
    text: "Mom.*"
    retryTapIfNoChange: true
- extendedWaitUntil:
    visible: { text: "I reached out" }
    timeout: 15000
- assertVisible: { text: "Reached out today" }
- assertVisible: { text: ".*Reach-out reminder: Monthly.*" }
- scrollUntilVisible:
    element: { text: ".*I snapped at her on the phone last weekend.*" }
    direction: DOWN
- assertVisible: { text: ".*I snapped at her on the phone last weekend.*" }
- assertVisible: { text: "Something to share" }
- tapOn:
    text: "Back"
    retryTapIfNoChange: true
- extendedWaitUntil:
    visible: { text: "Mom.*" }
    timeout: 15000
- tapOn:
    text: "HTC"
    retryTapIfNoChange: true
- waitForAnimationToEnd:
    timeout: 2500
- assertNotVisible: { text: "Got it" }
- assertVisible: { text: "Start a conversation" }
