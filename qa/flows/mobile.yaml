# GENERATED by scripts/qa/compile-flow.mjs — DO NOT EDIT.
# Source of truth: qa/journey.json + qa/selectors.json.
# Re-generate:  node scripts/qa/compile-flow.mjs
# Validate:     node scripts/qa/lint-flows.mjs   (fails if this file is stale)
#
# STORE is supplied per-run via --env=STORE=<store>. There is deliberately NO
# flow-level env: block — a baked default out-ranks the CLI flag and writes
# captures to the wrong store dir (hard-won; see runbooks/qa-capture.md).

appId: com.joshapproved.tend
---
- launchApp:
    clearState: true
- waitForAnimationToEnd:
    timeout: 8000
- takeScreenshot: qa/captures/${STORE}/00-launch
- extendedWaitUntil:
    visible: { text: "Today" }
    timeout: 30000
- extendedWaitUntil:
    visible: { text: "Mom.*" }
    timeout: 15000
- takeScreenshot: qa/captures/${STORE}/home
- tapOn: { text: "Mom.*" }
- extendedWaitUntil:
    visible: { text: "I reached out" }
    timeout: 10000
- takeScreenshot: qa/captures/${STORE}/person
- assertNotVisible: { text: "Reached out today" }
- tapOn: { text: "I reached out" }
- assertVisible: { text: "Reached out today" }
- tapOn: { text: "How often: Weekly" }
- extendedWaitUntil:
    visible: { text: "How often do you want to reach out?" }
    timeout: 10000
- takeScreenshot: qa/captures/${STORE}/person-cadence
- tapOn: { text: "Monthly" }
- assertNotVisible: { text: "How often do you want to reach out?" }
- assertVisible: { text: ".*How often: Monthly.*" }
